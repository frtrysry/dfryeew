apiVersion: v1
kind: Service
metadata:
  name: dfryeew
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dfryeew"
    port: 8080
  selector:
    app: dfryeew
